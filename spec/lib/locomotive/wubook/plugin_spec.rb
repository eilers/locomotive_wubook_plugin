require 'spec_helper'

module Locomotive
  module WuBook
    describe Plugin do

      let(:config) { Hash.new }

      let(:plugin) { Plugin.new }

      before(:each) do
      end

      it 'should authenticate if the page path matches the regex' do
      end

      protected

    end
  end
end
